# Changelog

## v1.0.5

- 变更：牌面图默认显示尺寸由 `48x72` 调小为 `32x48`。
- 新增：`card_image_size` 配置项，可自行调整牌面显示尺寸（如 `24x36`）。


## v1.0.4

- 新增：公开牌桌改用 QQ Markdown 内嵌牌图（黑底白字 / 白底黑字 / 蓝色背面），并去掉牌位序号，玩家自行数位置。
- 新增：私密手牌保持纯文字并去掉序号，更省字数。
- 新增：`card_image_base` 配置项，可替换牌面图片服务；图片过多时自动退回纯文字牌桌。
- 修复：公开播报里的牌面也改用图片，与牌桌一致。


## v1.0.3

- 变更：百搭牌改为抽到即 **随机落位**，不再要求玩家公开选择位置（去掉「选择百搭位置」按钮与 `放` 指令）。


## v1.0.2

- 修复：回合轮换后，回复里的「猜牌 / 收手 / 选择百搭位置」按钮仍按上一位玩家生成，
  导致新一轮玩家看不到自己的操作按钮、只能手动打指令。现在按钮始终按当前回合玩家生成。


## v1.0.1

- 修复：猜错时播报的“亮出线索牌”误用了对手的底牌（既显示错牌又会泄露对手牌面），
  现改为猜错者本回合抽到的线索牌。
- 优化：精简菜单与回合提示文案，玩法说明只在玩家主动查询「帮助 / 规则」时给出。

## v1.0.0

- 首个版本：QQ 官方群聊《达芬奇密码》，`only_for` 私密手牌按钮 + 完整牌局流程。
